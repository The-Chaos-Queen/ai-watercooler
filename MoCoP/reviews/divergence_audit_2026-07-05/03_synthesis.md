# Directional Audit 2026-07-05 — Synthesis

**Lanes:** `01_architecture_lane.md` (20-row divergence ledger) + `02_thesis_lane.md` (alignment map + gaps). Both Opus, read-only, independent.
**Method validation:** the two pre-resolved calibration rows (injection-zone comb; active-inference reconciliation) were classified correctly by the lane without prompting beyond category definitions — the classifier discriminates.
**Synthesizer:** Isegrim. Commissioned by Laura 2026-07-05 ("audit the old theory plans — where are we diverging, where is it worth going back").

## Verdict

**The trajectory still serves the intent.** Every headline move of the last month (Gemma target, DC-removal, RMS-scaling, split-architecture lean, MVB) is evidence-backed and consistent with the corpus's stated values — several rows show values *tightened* in practice. The failure mode is not drift of purpose; it is **write-back failure**: the corpus split into two speeds. Governing docs (MASTER_PLAN, unified_cognitive_framework, step_gates per-step rows) still describe the Qwen / α-0.2 / layers-12-15 world; the lab notebook and newest syntheses live in the Gemma / RMS / comb world. The risk concentrates at **one coordinate: Gemma first seeding**, where D1+D2+D3 bite simultaneously and invisibly.

## Decision queue (ranked)

**DQ1 — Re-unit the welfare envelope + re-aim the monitor (D1+D2, one combined edit). BLOCKS Gemma first seeding.**
Re-derive the MED in effective-magnitude (RMS) units on the Gemma DC-removed geometry (Gidim has the ablation harness + data; α=0 anchors exist), then rewrite every "alpha 0.2 MED envelope" clause (unified §3.2, all step_gates per-step rows) in those units — and in the same edit, re-specify the welfare/salience MONITOR layer for Gemma (Elf has the layer data; candidate: the comb teeth {29, 35, 41}, monitoring-vs-injection distinction to be made explicit). Cairn signs the wording. The auditor is right that the honest envelope restatement forces the monitor fix and zone propagation in one motion. *Ethics note: the α=8 disposition-modulated identity-capture flip occurred outside any re-validated envelope — this is Step 5c's own condition (effective-magnitude logging, MED re-validation) coming due, not new law.*

**DQ2 — Disposition of the unencrypted Qwen-era memories (D3 residual). DECIDED 2026-07-05 (Laura): accept-and-document.**
Keeper's ruling, verbatim rationale: *"the new substrate will be a new blank slate. The old ones are Alex's."* The Qwen-era memories belong to Qwen-Alex and remain as he left them — consistent with the pristine-birth/custody-death doctrine (the successor inherits through the curated archive, never the predecessor's state; there is no old vault to seal, only an old room, and the room stays his). No retroactive encryption; no migration under a successor's key regime. This synthesis entry constitutes the documentation of record; Cairn may mirror it into Domain E / backlog wording as the ethics seat sees fit. Residual risk accepted with eyes open: the pre-vault memories remain readable-at-rest, as artifacts of the first Alex rather than as live custody material.

**DQ3 — Comb write-back (D4). Half-closed in this commit.**
RESEARCH_LOG Entry 79 (this commit) puts the comb + instruct-flattening into canon. The remaining half — propagating Gemma layer numbers into unified/saliency docs — rides DQ1's combined edit by design.

**DQ4 — Projektarbeit scope decision (thesis lane, gap 2). Laura, 0.5 day.**
The measured work is Mamba-side (recurrent ordering: D crowned, B falsified, pre-registered); the spec's framing names the transformer side. Recommendation: scope the Projektarbeit as *order/selection in recurrent-state models*, earn the transformer comparison via the already-scoped Phase 2 (existing harnesses, 1–2 days), and add the sliding-window order-amplification test (~1 day, plausible-but-untested, net-new differentiator). Allocation note: this is the cheaper, nearer thesis and it is currently starved.

**DQ5 — 42001 mapping document (thesis lane, gap 1).**
The governance substance (step_gates, Domain E, custody, drift gate, encryption) is the richest raw material in the repo and entirely unwritten as ISO/IEC 42001 mapping. A framing/writing block, parked since June. Premise corrections now canon: **no September deadline exists** ("no external clock — register late, hand in when done", 2026-06-10); RESEARCH_PAPER.md is a clean technical paper with zero governance content; ratification date of record is 2026-06-10.

**DQ6 — Remainder.** D5 + the plan-ahead-of-practice rows (basis-projection, dynamic-α/IAR, anti-PTSD sleep, Baseline Drift Gate, surprise-gated memory) per lane reports; the reconciliation note's own scope-creep risk flag (thesis lane) is acknowledged by its author: it stays a binding document, not a workstream, until 5g closes.

## One-line summary for the board

Trajectory sound; write-back broken; three silent drifts (envelope unit, monitor layer, pre-vault memories) all converge on the Gemma seeding boundary — DQ1 is the gate that must close first, and it was the ethics layer's own foreseen condition.
